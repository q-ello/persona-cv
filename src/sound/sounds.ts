import cursorPath from '../assets/audio/00000_cursor.wav'
import cancelPath from '../assets/audio/cancel.wav'

console.log(cursorPath)

export const UISounds = {
    cursor: cursorPath,
    cancel: cancelPath
}